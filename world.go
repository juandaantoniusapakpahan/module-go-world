package modulegoworld

func World(name string, address string) string {
	return "Hello World " + "I am " + name + ", I am from " + address
}

func Name(name string) string {
	return "Hello " + name
}
